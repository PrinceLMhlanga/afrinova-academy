import 'package:flutter/material.dart';

// Stub for platforms that don't support web recording
// Returns null — the upload screen will show a recording guide instead
Future<Map<String, dynamic>?> openWebRecorder(BuildContext context) async {
  return null;
}