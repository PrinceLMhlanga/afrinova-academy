import 'package:flutter/material.dart';

// This stub is used on non-web platforms
// The actual web player is in web_player.dart (imported only on web)

Widget buildVideoPlayer(String videoUrl, VoidCallback? onVideoEnded) {
  // This will never be called — it exists only so the compiler is happy
  return const SizedBox.shrink();
}